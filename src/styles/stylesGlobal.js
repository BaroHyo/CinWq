import { StyleSheet } from "react-native";

const stylesGlobal = StyleSheet.create({
  container: {
    flex: 1,
    marginTop: 5,
  },
  backButton: {
    position: "absolute",
    left: 20,
  },
});

export default stylesGlobal;
